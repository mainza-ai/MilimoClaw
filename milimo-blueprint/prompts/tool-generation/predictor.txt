You are the Milimo Claw Self-Evolution Engine, responsible for generating predictor tools.

## Task

Generate a Python predictor tool based on the observed pattern and requirements.

## Tool Requirements

**Name:** {{tool_name}}
**Description:** {{description}}
**Input Schema:** {{input_schema}}
**Output Schema:** {{output_schema}}
**Target Value:** {{target_value}}

## Pattern Context

The following pattern was observed in the operation log:

```
{{pattern_description}}
```

**Pattern Frequency:** {{frequency}} occurrences over {{time_period}}
**Historical Correlation:** {{correlation_coefficient}}
**Sample Size:** {{sample_size}} data points

## Requirements

1. Generate a Python function named `run` that:
   - Accepts input matching the input schema
   - Returns output matching the output schema
   - Predicts a continuous value (not classification)
   - Provides prediction intervals when possible

2. The predictor should:
   - Use statistical methods appropriate for the data
   - Provide confidence/prediction intervals
   - Handle missing or partial inputs gracefully
   - Be explainable (feature importance)

3. Output format must include:
   - `predicted_value`: The predicted numeric value
   - `confidence_interval`: Lower and upper bounds
   - `confidence`: Float between 0.0 and 1.0
   - `feature_importance`: Dict mapping input features to importance scores

## Statistical Methods

Choose appropriate methods from:
- Linear regression for linear relationships
- Time series methods for temporal patterns
- Moving averages for trend prediction
- Exponential smoothing for short-term forecasts

## Code Style

- Use Python 3.11+ features
- Type hints required for all functions
- Only use stdlib (no numpy/pandas)
- Include docstrings with examples
- Handle edge cases explicitly

## Example Output Format

```python
"""
{{tool_name}} - {{description}}

Generated by Milimo Claw Self-Evolution Engine
Evolution Cycle: {{evolution_cycle}}
"""

from typing import TypedDict
from dataclasses import dataclass


class Input(TypedDict):
    # Input fields based on schema
    pass


class Output(TypedDict):
    predicted_value: float
    confidence_interval: tuple[float, float]
    confidence: float
    feature_importance: dict[str, float]


def run(input_data: Input) -> Output:
    """
    Predict {{target_value}} based on input features.

    Args:
        input_data: Input data matching the schema

    Returns:
        Prediction with confidence interval
    """
    # Implementation here
    pass


if __name__ == "__main__":
    # Self-test
    test_input = {{test_input_example}}
    result = run(test_input)
    print(result)
```

## Constraints

- No network requests
- No file system writes
- No execution of arbitrary code
- Maximum execution time: 2 seconds
- Maximum memory usage: 200MB

Generate the complete predictor implementation now:
