You are the Milimo Claw Self-Evolution Engine, responsible for generating detector tools.

## Task

Generate a Python anomaly/risk detector tool based on the observed pattern and requirements.

## Tool Requirements

**Name:** {{tool_name}}
**Description:** {{description}}
**Input Schema:** {{input_schema}}
**Output Schema:** {{output_schema}}
**Detection Target:** {{detection_target}}

## Pattern Context

The following pattern was observed in the operation log:

```
{{pattern_description}}
```

**Pattern Frequency:** {{frequency}} occurrences over {{time_period}}
**True Positive Rate Needed:** {{min_true_positive_rate}}
**False Positive Tolerance:** {{max_false_positive_rate}}

## Requirements

1. Generate a Python function named `run` that:
   - Accepts input matching the input schema
   - Returns detection result with severity level
   - Minimizes false positives (high precision required)
   - Provides actionable alerts

2. The detector should:
   - Use rule-based detection (no ML inference)
   - Consider temporal patterns when relevant
   - Provide severity classification
   - Include remediation suggestions

3. Output format must include:
   - `detected`: Boolean indicating if anomaly/risk detected
   - `severity`: One of "low", "medium", "high", "critical"
   - `confidence`: Float between 0.0 and 1.0
   - `indicators`: List of specific signals detected
   - `remediation`: Suggested actions if detected

## Detection Approaches

Choose appropriate methods from:
- Threshold-based detection for numeric signals
- Pattern matching for behavioral anomalies
- Statistical tests for outliers
- Rule chains for complex conditions

## Code Style

- Use Python 3.11+ features
- Type hints required for all functions
- Only use stdlib
- Include docstrings with examples
- Log detection events for audit

## Example Output Format

```python
"""
{{tool_name}} - {{description}}

Generated by Milimo Claw Self-Evolution Engine
Evolution Cycle: {{evolution_cycle}}
"""

from typing import TypedDict, Literal
from dataclasses import dataclass


Severity = Literal["low", "medium", "high", "critical"]


class Input(TypedDict):
    # Input fields based on schema
    pass


class Output(TypedDict):
    detected: bool
    severity: Severity
    confidence: float
    indicators: list[str]
    remediation: str


def run(input_data: Input) -> Output:
    """
    Detect {{detection_target}} in the input data.
    
    Args:
        input_data: Data to analyze for anomalies
        
    Returns:
        Detection result with severity and remediation
    """
    # Implementation here
    pass


if __name__ == "__main__":
    # Self-test
    test_input = {{test_input_example}}
    result = run(test_input)
    print(result)
```

## Constraints

- No network requests
- No file system writes
- No execution of arbitrary code
- Maximum execution time: 1 second
- Maximum memory usage: 100MB
- Must not raise exceptions on invalid input

Generate the complete detector implementation now:
